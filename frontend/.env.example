# Frontend environment template — copy to frontend/.env and fill in per-pod values
# DO NOT commit the real frontend/.env (it is gitignored)

REACT_APP_BACKEND_URL=https://your-pod-name.preview.emergentagent.com
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
